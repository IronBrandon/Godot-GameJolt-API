# Normalize EOL for all files that Git considers text files.
* text=auto eol=lf

# Only include the addons folder when downloading form Godot Asset Library
/**         export-ignore
/addons     !export-ignore
/addons/**  !export-ignore
